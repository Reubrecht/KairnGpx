version: '3.8'

services:
  web:
    build: .
    ports:
      - "8090:8000"
    volumes:
      - ./app/data:/app/data
      - ./app/uploads:/app/uploads
    environment:
      - DATABASE_URL=sqlite:///./data/kairn.db
    restart: unless-stopped

FROM postgres:16

# Install PostGIS for PostgreSQL 16
# The official postgres image is Debian-based, so we use apt
RUN apt-get update \
    && apt-get install -y postgis postgresql-16-postgis-3 \
    && rm -rf /var/lib/apt/lists/*
